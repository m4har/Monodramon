# Monodramon

## Learn mono repo with nextjs & lerna

how to run

```bash
#install
  > npm install && npm run bootsrap

 # run parallel
  > npm start
```

```bash
#port usage
3000 = main
3001 = auth
3002 = profile
```
